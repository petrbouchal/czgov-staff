---
title: "SankeyPlots"
output: html_document
---

This is an R Markdown document. Markdown is a simple formatting syntax for authoring HTML, PDF, and MS Word documents. For more details on using R Markdown see <http://rmarkdown.rstudio.com>.

When you click the **Knit** button a document will be generated that includes both content as well as the output of any embedded R code chunks within the document. You can embed an R code chunk like this:

```{r Sankey Google Charts}
require(googleVis)
rozp <- read.csv('./data-input/RozpoctovaRegulace.csv')
plot (
  gvisSankey(from = 'source',to = 'target', weight = 'value', data = rozp,
             options = list(width=800))
  )

```


```{r rChart Sankey Verejny sektor}
library(rCharts)
rozp <- read.csv('./data-input/VerejnySektor.csv')
colnames(rozp) <- c('source','target','value')
sankeyPlotVS <- rCharts$new()
sankeyPlotVS$setLib('./src/rCharts_d3_sankey-gh-pages/')
sankeyPlotVS$setTemplate(script = "./src/rCharts_d3_sankey-gh-pages/layouts/chart.html")

sankeyPlotVS$set(
  data = rozp,
  nodeWidth = 5,
  nodePadding = 10,
  layout = 99,
  width = 750,
  height = 500
)
 
sankeyPlotVS
sankeyPlotVS$print('blah', include_assets = TRUE, cdn=TRUE)
```

```{r rChart Sankey Rozpocet}
library(rCharts)
rozp <- read.csv('./data-input/RozpoctovaRegulace.csv')
colnames(rozp) <- c('source','target','value')
sankeyPlotR <- rCharts$new()
sankeyPlotR$setLib('./src/rCharts_d3_sankey-gh-pages/')
sankeyPlotR$setTemplate(script = "./src/rCharts_d3_sankey-gh-pages/layouts/chart.html")

sankeyPlotR$set(
  data = rozp,
  nodeWidth = 5,
  nodePadding = 10,
  layout = 99,
  width = 750,
  height = 500
)
 
sankeyPlotR
sankeyPlotR$print('blah', include_assets = TRUE, cdn=TRUE)
```

Note that the `echo = FALSE` parameter was added to the code chunk to prevent printing of the R code that generated the plot.
